services:
  - type: web
    name: real-esl-app
    env: python
    rootDir: .
    buildCommand: "pip install -r requirements.txt"
    startCommand: "gunicorn app.main:app"
    envVars:
      - key: FLASK_ENV
        value: production
    autoDeploy: true
